remove_package:
  name: Remove Package
  description: Remove a package from monitoring. The tracking number will be suppressed from future syncs.
  fields:
    tracking_number:
      name: Tracking Number
      description: The package tracking number to remove.
      required: true
      example: "1Z999AA10123456784"
      selector:
        text:
